requests>=2.31
certifi  # explicit, not just a transitive dep of requests -- sources/ocds_client_base.py
         # merges its bundle with sources/extra_ca_certs/ at import time.
pip-system-certs>=4.0; sys_platform == 'win32'  # Windows only -- hooks the OS cert
                       # store, which doesn't exist on Linux (Render). Fixes 403/TLS
                       # failures against api.publiccontractsscotland.gov.uk /
                       # api.sell2wales.gov.wales on Windows dev machines. The Linux
                       # (Render) fix predicted here turned out to be a genuinely
                       # missing intermediate CA on both sites' own TLS setup -- see
                       # ocds_client_base.py's module docstring, confirmed live
                       # 2026-08-10 and fixed by supplying it via extra_ca_certs/.
rapidfuzz>=3.6
openpyxl>=3.1
python-dotenv>=1.0
pytest>=8.0
flask>=3.0
flask-login>=0.6
flask-wtf>=1.2  # CSRF protection app-wide, see dashboard/__init__.py
flask-limiter>=3.5  # login rate-limiting, see dashboard/auth.py
sentry-sdk[flask]>=2.0  # error tracking, only active if SENTRY_DSN is set
anthropic>=0.40
openai>=1.0  # Phase 2 scope reads, alternate provider -- see SCOPE_READ_PROVIDER in .env
reportlab>=4.0  # Internal Addendum / Capture Brief PDF generation, see escalation/brief.py
pypdf>=4.0  # PDF text extraction in tests only (test_brief.py), not used at runtime
waitress>=3.0  # production WSGI server, see wsgi.py
APScheduler>=3.10  # in-process daily sweep, see scheduler.py
python-docx>=1.1  # Weekly/Monthly Trifork reports, see reporting/reports.py
